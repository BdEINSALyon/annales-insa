<div class="row">
  <h2>Arrète de ramer avec Annales INSA</h2>

  <%= form_for("user", :url => user_session_path) do |f| %>
    <%= f.text_field :email %>
    <%= f.password_field :password %>
    <%= f.check_box :remember_me %>
    <%= f.label :remember_me %>
    <%= f.submit 'Sign in', class: 'button' %>
  <% end %>
</div>